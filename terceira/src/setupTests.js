// jest-dom adiciona "matchers" personalizados do Jest para testar elementos do DOM.
// Exemplo: permite escrever assertions mais legíveis e diretos, como:
// expect(element).toHaveTextContent(/react/i)

// Importa o pacote para disponibilizar os matchers no ambiente de testes
import '@testing-library/jest-dom';